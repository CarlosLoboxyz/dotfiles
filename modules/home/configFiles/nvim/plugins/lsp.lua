-- In a Nix setup, you call setup() for each server provided in extraPackages
local lspconfig = require("lspconfig")

-- Example: Add your servers here
-- lspconfig.nil_ls.setup({}) 
-- lspconfig.vtsls.setup({})
-- lspconfig.gopls.setup({})
